var assert, skyll;

assert = require('assert');

skyll = require('./index');

describe('skyll', function() {
  return it('should have unit test!', function() {
    return assert(false, 'we expected this package author to add actual unit tests.');
  });
});
